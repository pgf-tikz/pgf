-- Tests for functionality that requires the LuaTeX engine which currently
-- includes the graph drawing (gd) library and the luamath library

stdengine    = "luatex"
checkengines = {"luatex"}
testfiledir  = "testfiles-regression-luatex"
